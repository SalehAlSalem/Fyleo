# 🚀 دليل الإعداد السريع - Fyleo Setup Guide

## 🎯 نظام التخزين - Storage System

يستخدم **Fyleo** نظام تخزين يعتمد على **MinIO**، وهو حل تخزين سحابي مفتوح المصدر يمكنك استضافته بنفسك، مما يمنحك تحكماً كاملاً في ملفاتك وتكاليفك.

### 🗄️ MinIO Storage
- **جميع الملفات**: يتم تخزينها على خادم MinIO الخاص بك.
- **تحكم كامل**: أنت تملك بياناتك بالكامل.
- **قابلية للتوسع**: يمكنك زيادة مساحة التخزين حسب الحاجة.

---

## ⚙️ خطوات الإعداد (بعد إعداد Appwrite)

### 1️⃣ إعداد MinIO

1. قم بتثبيت وتشغيل خادم MinIO (عبر Docker أو مباشرة).
2. أنشئ `bucket` جديد (مثلاً: `fyleo-storage`).
3. احصل على `Endpoint`, `Port`, `Access Key`, و `Secret Key`.
4. انسخ هذه البيانات إلى ملف `.env` الخاص بك.

### 2️⃣ تشغيل المشروع

```bash
# انسخ ملف الإعدادات
cp .env.example .env

# حدث الإعدادات بالقيم الحقيقية
# ثم شغل المشروع
npm run dev
```

---

## 🛠️ استكشاف الأخطاء

### خطأ في MinIO
```
Error: GitHub token invalid
```
**الحل**: تأكد من صحة GitHub Token

### خطأ في Supabase
```
Error: Supabase connection failed
```
**الحل**: تأكد من URL و anon key في Supabase

---

## 📊 مراقبة الاستخدام

### GitHub (مجاني)
- بدون حدود للملفات الصغيرة
- مراقب في GitHub Repository

### Supabase (1GB شهرياً)
- راقب الاستخدام في Supabase Dashboard
- ستحصل على تنبيه عند الاقتراب من الحد

---

## 🎉 جاهز للتشغيل!

بعد إعداد كل شيء، المشروع سيعمل على:
**http://localhost:5173**

---

*جامعة البلقاء التطبيقية - AlBalqa Applied University*